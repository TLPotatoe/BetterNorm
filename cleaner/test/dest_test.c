/*
 *  blabla
printf*/
//bdsjkjksdf
				//dsbfkjs

int	main(void)
{
	int	i;
	void	k;
	t_fdsf	test;

	coucou == jfio;
	coucou != dnslkf;
	dfs /= dfs;
	fds *= fds;
	fds += fds;
	fdsfs -= fdsds;
	i++;
	i--;
	i = dsffs + fds;
	fds = fds - fds;
	fdsfsd = ds / fds;
	return (1);
	test{{test}};
	test[pos[fdgd]];
}
