/*
 * blabla
printf*/
//bdsjkjksdf
				//dsbfkjs

int	main(void)
{
	/* test */
	vvuui
	coucou == jfio;
	coucou != dnslkf;
	dfs /= dfs;
	fds *= fds;
	fds += fds;
	fdsfs -= fdsds;
	i++;
	i--;
	i = dsffs + fds;
	fds = fds - fds;
	fdsfsd =  / fds;
	return (1);
	test{{test}};
	test[pos[fdgd]];
}
